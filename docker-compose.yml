services:
  marker-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: marker-api
    ports:
      - "8000:8000"
    environment:
      - TORCH_DEVICE=cpu # Change to 'cuda' if you have GPU support
      - PYTHONUNBUFFERED=1
    volumes:
      - ./temp_processing:/app/temp # Temporary processing directory
      - ./source_pdfs:/app/input # Mount input directory
      - marker_models:/root/.cache/datalab/models # Persist ML models
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s # Longer startup time for build
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 6G
        reservations:
          memory: 2G

volumes:
  temp_processing:
    driver: local
  marker_models:
    driver: local
